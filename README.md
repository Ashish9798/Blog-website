# My Awesome Blog (HTML Project)

This is a simple blog webpage created using **HTML only**.  
The project is beginner-friendly and demonstrates the use of basic HTML tags, lists, images, and page structure.

---

## 📂 Project Features

- Blog-style webpage
- Navigation links (Home, About, Contact)
- Two blog posts
- Image usage
- Ordered and unordered lists
- Clean and readable HTML structure

---

## 🏷️ HTML Tags Used

The following HTML tags are used in this project:

- `<html>` – Root element of the HTML document  
- `<head>` – Contains metadata  
- `<title>` – Page title  
- `<meta>` – Character set & viewport  
- `<body>` – Main content container  

### Text & Content Tags
- `<h1>` – Main heading  
- `<h2>` – Blog post titles  
- `<h3>` – Subheading  
- `<p>` – Paragraph text  
- `<br>` – Line break  
- `<hr>` – Horizontal line  

### Link & Media Tags
- `<a>` – Anchor tag for navigation links  
- `<img>` – Used to display image  

---

## 📋 Lists Used

### 1️⃣ Unordered List (`<ul>`)
Used for items without any specific order.

Example:
- Things seen in the park
- Pizza toppings

### 2️⃣ Ordered List (`<ol>`)
Used for items with a sequence or order.

Example:
- Favourite foods
- Pasta types

### 🔁 Nested Lists
- `<ul>` inside `<li>`  
- `<ol>` inside `<li>`  

Used to show sub-items like:
- Pizza → toppings  
- Pasta → types  

---

## 🛠️ Technologies Used

- HTML5

bhi bana deta hoon 👍
 
